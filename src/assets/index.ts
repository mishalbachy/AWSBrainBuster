import lambdaIcon from './images/aws-services/lambda.png';
import favicon from './icons/favicon.svg';

export const images = {
  services: {
    lambda: lambdaIcon
  }
};

export const icons = {
  favicon
};

export default {
  images,
  icons
};